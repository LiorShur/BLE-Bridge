{
  "//": "FULL app config. Requires the RN toolchain + node_modules installed (npm install). Checks the React Native, native-bridge, and Viro/AR layers. Run: npm run typecheck:app — on a dev machine, not in the pure-logic CI path.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2020", "DOM"],
    "types": ["react"],
    "allowJs": false,
    "noEmit": true
  },
  "include": ["src", "index.js"],
  "//exclude": "Stage-2 files import deps not installed in Stage 1 (Viro, compass). Re-include them when flipping config.AR_ENABLED / HEADING_ENABLED.",
  "exclude": [
    "node_modules",
    "**/*.test.ts",
    "**/*.test.tsx",
    "src/ar/BridgeScene.tsx",
    "src/sensors/useCompassHeading.ts"
  ]
}
