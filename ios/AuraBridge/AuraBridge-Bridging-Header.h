//
// Objective-C bridging header for the AuraBridge iOS target.
//
// When you add the first Swift file (BlePeripheral.swift) to the Xcode project,
// Xcode offers to create a bridging header — point it at this file (or let Xcode
// create one and paste this line in). It exposes React Native's bridge types
// (RCTPromiseResolveBlock, RCT_EXTERN_MODULE, …) to Swift so the native module
// compiles without an explicit `import React`.
//
#import <React/RCTBridgeModule.h>
