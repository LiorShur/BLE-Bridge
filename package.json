{
  "name": "aurabridge",
  "version": "0.0.1",
  "private": true,
  "description": "Connectionless BLE + AR 'bridge of light' PoC (Android). Pure-logic core is unit-tested; RN/native/AR layers build on a device toolchain.",
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:app": "tsc -p tsconfig.app.json --noEmit"
  },
  "//deps": "@reactvision/react-viro is PINNED to 2.41.6 — the last version whose react-native peer is '*' (2.42+ require RN 0.76+). The compass is a native module (no JS dep). The HUD slider is pure-JS.",
  "dependencies": {
    "@reactvision/react-viro": "2.41.6",
    "react": "18.2.0",
    "react-native": "0.74.5",
    "react-native-ble-plx": "^3.2.1",
    "zustand": "^4.5.4"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@react-native/babel-preset": "0.74.87",
    "@react-native/metro-config": "0.74.87",
    "@react-native/typescript-config": "0.74.87",
    "@types/react": "^18.2.6",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
